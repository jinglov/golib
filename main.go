package main

//import "github.com/jinglov/golib/lovlog"

func main() {
	/*	lovlog.SetLevel(lovlog.DEBUG)
		lovlog.SetRollingDaily("./", "test")
		lovlog.Error("error")
		lovlog.Debug("debug")
		lovlog.Info("info")
		lovlog.Warn("warn")
		fatal := []interface{}{"fatal1", "fatal2"}

		lovlog.Fatal(fatal...)
		lovlog.Fatal("fatal1", "fatal2")*/
	/*	a := make(chan int)
		go func() {
			fmt.Println(<-a)
		}()
		a <- 2*/
}
